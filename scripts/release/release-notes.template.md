Kurmancî (ku-Latn) language platform, release __VERSION__: engine __ENGINE__, C ABI __ABI__, pack schema __PACK_SCHEMA__, language-model schema __LM_SCHEMA__. Source commit `__COMMIT__`.

**Release bundle** `kurmanci-ku-Latn-__VERSION__.tar.gz` (production release built from a clean checkout of the source commit): the three packs (`seed` __SEED__ entries, `reviewed` __REVIEWED__ entries, `experimental-full` __EXPERIMENTAL__ entries), the committed language model (statistics only), `include/kurmanci.h`, `ATTRIBUTION`, `LICENSES/`, `compatibility.json`, `provenance.json`, `SHA256SUMS`. The extracted bundle is deterministic: `SHA256SUMS` and its sha256 `__SUMS__` identify the reproducible bundle contents, and `kurmanci-ku-Latn-__VERSION__.tar.gz.sha256` identifies the exact archive uploaded to this release. The published platform artifacts are attached and hashed as received: `android/kurmanci-android-__VERSION__.aar` sha256 `__AAR__`, `apple/KurmanciFFI-v__VERSION__.xcframework.zip` sha256 `__XC__`.

**SDKs published from the same commit** (verified before this release was created: both SDK tags resolve to `__COMMIT__`, both publication workflows succeeded, and the Apple release manifest and the Swift distribution manifest record that commit and this XCFramework)
- Android: `io.github.ferhatguneri:kurmanci-android:__VERSION__` on Maven Central (tag `android-v__VERSION__`).
- Apple: `https://github.com/Kurdi-Language/kurmanci-swift` tag `__VERSION__`, wrapping `KurmanciFFI-v__VERSION__.xcframework.zip` from release `swift-v__VERSION__`.

**Verify** with the vendor evaluation kit at the immutable tag `vendor-kit-__VERSION__` of this repository (commit `__KIT_COMMIT__`; `docs/vendor-evaluation-kit.md` there; `scripts/vendor/evaluate.sh fetch --version __VERSION__` checks the archive against its `.sha256`, the exact file set against `SHA256SUMS`, the release identity, and the published AAR and XCFramework against `provenance.json`). Without the kit: `shasum -a 256 -c kurmanci-ku-Latn-__VERSION__.tar.gz.sha256`, unpack, and `shasum -a 256 -c SHA256SUMS` inside the bundle. Licensing: code Apache-2.0; data as recorded per source in `provenance.json` and `ATTRIBUTION`; the owner's licensing stance is in `NOTICE`. Changes: `CHANGELOG.md`, section __VERSION__.
